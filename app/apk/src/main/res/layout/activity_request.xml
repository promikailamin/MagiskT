<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="view_model"
            type="pro.magisk.ui.surequest.SuRequestViewModel" />

    </data>

    <com.google.android.material.card.MaterialCardView
        style="@style/WidgetFoundation.Card.Elevated"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center">

        <LinearLayout
            android:id="@+id/su_popup"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minWidth="350dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/request_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="@dimen/l1"
                android:layout_marginBottom="@dimen/l_50"
                android:gravity="center_horizontal"
                android:text="@string/su_request_title"
                android:textAppearance="@style/AppearanceFoundation.Title" />

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="@dimen/l_50"
                android:layout_marginBottom="@dimen/l_50"
                android:orientation="horizontal"
                android:paddingStart="10dp"
                android:paddingEnd="10dp">

                <ImageView
                    android:id="@+id/app_icon"
                    style="@style/WidgetFoundation.Icon"
                    android:layout_gravity="center_vertical"
                    android:layout_marginStart="@dimen/l_50"
                    android:layout_marginEnd="@dimen/l1"
                    android:layout_weight="0"
                    android:padding="0dp"
                    android:src="@{view_model.icon}"
                    app:tint="@null"
                    tools:src="@drawable/ic_delete_md2" />

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center_vertical"
                    android:layout_weight="1"
                    android:gravity="center_vertical"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/app_name"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:ellipsize="end"
                        android:maxWidth="300dp"
                        android:maxLines="1"
                        android:minWidth="200dp"
                        android:text="@{view_model.title}"
                        android:textAppearance="@style/AppearanceFoundation.Body"
                        android:textStyle="bold"
                        tools:text="Magisk" />

                    <TextView
                        android:id="@+id/package_name"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:ellipsize="end"
                        android:maxWidth="300dp"
                        android:maxLines="1"
                        android:minWidth="200dp"
                        android:text="@{view_model.packageName}"
                        android:textAppearance="@style/AppearanceFoundation.Caption.Variant"
                        tools:text="pro.magisk" />
                </LinearLayout>
            </LinearLayout>

            <Spinner
                android:id="@+id/timeout"
                onTouch="@{() -> view_model.spinner_touched()}"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:enabled="@{view_model.grant_enabled}"
                app:items="@{@stringArray/allow_timeout}"
                app:layout="@{@layout/item_spinner}"
                android:selection="@={view_model.selectedItemPosition}" />

            <TextView
                android:id="@+id/warning"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_margin="@dimen/l1"
                android:gravity="center"
                android:text="@string/su_warning"
                android:textAppearance="@style/AppearanceFoundation.Body"
                android:textColor="?colorError"
                android:textStyle="bold"
                tools:text="@string/su_warning" />

            <LinearLayout
                style="?android:buttonBarStyle"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="bottom"
                android:orientation="horizontal"
                android:paddingStart="@dimen/l2"
                android:paddingEnd="@dimen/l2">

                <Button
                    android:id="@+id/deny_btn"
                    style="@style/WidgetFoundation.Button.Text"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:onClick="@{() -> view_model.deny_pressed()}"
                    android:text="@{view_model.deny_text}"
                    android:textColor="?colorOnSurfaceVariant"
                    tools:text="@string/deny" />

                <Button
                    android:id="@+id/grant_btn"
                    style="@style/WidgetFoundation.Button.Text"
                    onTouch="@{view_model.grant_touch_listener}"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:onClick="@{() -> view_model.grant_pressed()}"
                    android:enabled="@{view_model.grant_enabled}"
                    android:text="@string/grant" />

                <requestFocus />
            </LinearLayout>

        </LinearLayout>

    </com.google.android.material.card.MaterialCardView>

</layout>
